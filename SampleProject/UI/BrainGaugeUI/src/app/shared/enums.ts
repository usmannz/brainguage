export enum SideMenu {
    Dashboard = 1,
    Admin = 12,
    User = 13,
    Questions = 2
}

export enum Roles {
    Admin = 1,
    User = 2
}

export enum PageAccessType {
    PUBLIC = 0,
    PRIVATE = 1
}

export enum Pages {
    None = 0,
    Dashboard = 1,
    Questions = 2
}